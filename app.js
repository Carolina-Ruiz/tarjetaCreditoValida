function isValidCard (number,tarjeta) {
	var credit = prompt("Ingrese el número de la tarjeta de crédito")
	var long= number.length;
	var number = number_tarjeta.toString();
	var cifra = 
		for (var i=0; i < longitud; i+=2){ //orden inverso
		cifra = parseInt(number.charAt(i))*2; // se multiplica por dos según el algoritmo de Luhn
		if (cifra >=10){  // si el doble del número es mayo o igual a 10 se suman cada digitos del resultado.
		cifra = parseInt(cifra.charAt + parseInt(cifra_charAt);
	{suma+=cifra;    // suma de todos los digitos de la tarjeta
	}
		if ((suma % 10) === 0){   // el residuo de la división con el número 10 debe resulta 0 para demostrar que la tarjeta es valida.
	alert("Número de tarjeta correcto");
} 	else {
	alert("El número de tarjeta no es válido");
}
} 		if (cadena ===""){
	alert("ingresa una opción valida"); // el usuario no bede ingresar campos vacíos.
}

}





